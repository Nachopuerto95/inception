# Acepta conexiones desde cualquier IP (necesario para que WordPress se conecte)
bind 0.0.0.0
# Puerto por defecto de Redis
port 6379
# Desactiva modo protegido para permitir conexiones sin contraseña desde la red interna
protected-mode no
